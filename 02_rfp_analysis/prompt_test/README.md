# Solar 파일럿 — 02 RFP 분석

`02/`에는 사전규격·입찰공고의 고정 입력, 프롬프트, 실행 코드와 10회 실행 결과가 있다. 실행 전 `config.example.env`를 복사해 이 폴더에 `.env`를 만들고 API 키를 설정한다.

```powershell
python .\02_rfp_analysis\prompt_test\02\run.py
```

실행 결과와 해석은 `02/experiment_report.md` 및 `02/run/`에서 확인한다.
