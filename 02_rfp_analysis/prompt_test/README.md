# Solar 파일럿 — 02 RFP 분석

이 폴더에는 사전규격·입찰공고의 고정 입력, 프롬프트, 실행 코드와 10회 실행 결과가 있다. Solar·Langfuse 설정은 저장소 루트의 `.env`에서 읽고, 비밀값 없는 항목은 루트의 `solar.config.example.env`를 참고한다.

```powershell
python .\02_rfp_analysis\prompt_test\run.py
```

실행 결과와 해석은 `experiment_report.md` 및 `runs/`에서 확인한다.
