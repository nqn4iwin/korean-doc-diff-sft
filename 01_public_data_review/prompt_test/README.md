# Solar 파일럿 — 01 공공데이터 활용 사전검토

`01/`에는 고정 근거 묶음, 프롬프트, 실행 코드와 10회 실행 결과가 있다. API 설정은 이 폴더의 `.env`에서 읽고, 비밀값 없는 항목은 `config.example.env`를 참고한다.

```powershell
python .\01_public_data_review\prompt_test\01\run.py
```

실행 결과와 해석은 `01/experiment_report.md` 및 `01/run/`에서 확인한다.
